<h1 style="text-align:center; margin-bottom: 2rem;">Editing Organization</h1>

<div class="formBody">
    <div class="customForm">
        <%= render 'form' %>
    </div>
</div>

<div class="formBody" style="margin-top:2%">
    <%= link_to 'Show', @organization, {:style=>'color:#FFFFFF text-decoration:none;', :class => "btn btn-secondary btn-sm"} %> |
    <%= link_to 'Back', :back, {:style=>'color:#FFFFFF text-decoration:none;', :class => "btn btn-secondary btn-sm"}   %>
</div>
